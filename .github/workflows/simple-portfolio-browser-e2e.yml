name: Simple Portfolio Browser E2E

on:
  pull_request:
    branches:
      - main
    paths:
      - 'src/components/Portfolio.tsx'
      - 'src/components/SimplePortfolioView.tsx'
      - 'src/lib/simplePortfolio.ts'
      - 'src/components/Sidebar.tsx'
      - 'scripts/simple-portfolio-browser-e2e.mjs'
      - '.github/workflows/simple-portfolio-browser-e2e.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  simple-portfolio-browser-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Production build
        run: bun run build

      - name: Verify simple portfolio and goal ratings at 390 CSS pixels in real Chrome
        env:
          NODE_ENV: production
          PORT: 3100
          APP_URL: http://localhost:3100
          SESSION_SECRET: ci-simple-portfolio-session-secret-2026-at-least-32-characters
          LEHRERAPP_ACCESS_TEAM: ci-simple-portfolio-access-code-2026
          LEHRERAPP_ACCESS_EXTERNAL: ci-simple-portfolio-external-code-2026
          KLASSIO_DATA_DIR: /tmp/klassio-simple-portfolio-e2e-data
          KLASSIO_E2E_BASE_URL: http://localhost:3100
          KLASSIO_E2E_ACCESS_CODE: ci-simple-portfolio-access-code-2026
          KLASSIO_CHROME_DEBUG_URL: http://127.0.0.1:9253
          KLASSIO_E2E_SCREENSHOT: /tmp/klassio-simple-portfolio-e2e.png
        run: |
          set -euo pipefail
          rm -rf /tmp/klassio-simple-portfolio-e2e-data /tmp/klassio-simple-portfolio-chrome
          mkdir -p /tmp/klassio-simple-portfolio-e2e-data

          node dist/server.cjs > /tmp/klassio-simple-portfolio-server.log 2>&1 &
          SERVER_PID=$!

          CHROME_BIN="$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser || true)"
          if [ -z "$CHROME_BIN" ]; then
            echo "No Chrome/Chromium binary found on runner."
            exit 1
          fi

          "$CHROME_BIN" --headless=new --no-sandbox --disable-gpu --disable-dev-shm-usage \
            --remote-debugging-port=9253 --remote-allow-origins=* \
            --user-data-dir=/tmp/klassio-simple-portfolio-chrome about:blank \
            > /tmp/klassio-simple-portfolio-chrome.log 2>&1 &
          CHROME_PID=$!

          cleanup() {
            kill "$CHROME_PID" 2>/dev/null || true
            kill "$SERVER_PID" 2>/dev/null || true
          }
          trap cleanup EXIT

          for i in {1..60}; do
            if curl --fail --silent http://localhost:3100/api/health >/tmp/klassio-simple-portfolio-health.json; then
              break
            fi
            sleep 0.5
          done
          grep -q '"status":"ok"' /tmp/klassio-simple-portfolio-health.json
          bun scripts/simple-portfolio-browser-e2e.mjs

      - name: Upload browser screenshot and diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: klassio-simple-portfolio-browser-${{ github.sha }}
          path: |
            /tmp/klassio-simple-portfolio-e2e.png
            /tmp/klassio-simple-portfolio-server.log
            /tmp/klassio-simple-portfolio-chrome.log
          if-no-files-found: ignore
          retention-days: 7