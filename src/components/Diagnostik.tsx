import React from 'react';
import { DiagnosticContainer } from './diagnostics/DiagnosticContainer';

export const Diagnostik: React.FC = () => {
  return <DiagnosticContainer />;
};

export default Diagnostik;
